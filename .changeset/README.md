# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `npx changeset` to record a change; the release workflow consumes these files
to version and publish the package.
